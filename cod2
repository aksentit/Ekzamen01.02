  Console.WriteLine("Введите заданное количество чисел");
            for (int i = 1; i <= n; i++)
            {
                x = Convert.ToInt32(Console.ReadLine());
                sum += x;
            }
            rezult = sum / n;
